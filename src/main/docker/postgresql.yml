version: '2'
services:
  notey-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Notey/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Notey
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
